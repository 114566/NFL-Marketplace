

const walletApiUrl = process.env.WALLET_ACCESS_API_URL
const walletIcon = process.env.WALLET_ICON
const walletDiscovery = process.env.WALLET_DISCOVERY

const publicConfig = {
  walletApiUrl,
  walletDiscovery,
  walletIcon
}

export default publicConfig